"use server";

// TODO (Part 4 Step 8.5): wire this Server Action to clear the JWT cookie
// (lib/auth.ts `clearJwt`) and redirect to /login.

export async function logoutAction() {
  console.log("[starter] logoutAction not wired yet");
}
